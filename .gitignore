# Rust
/target
**/target
# Cargo.lock IS committed (matches sibling crates; reproducible CI)

# Node / pnpm
node_modules
**/node_modules
.pnpm-store
*.tsbuildinfo

# Editor / OS
.DS_Store
.idea
.vscode
*.log

# Build output (TS package emits to dist/ only at publish time)
dist
**/dist
