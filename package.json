{
  "name": "wavekat-flow-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "Single-source-of-truth for the WaveKat call-flow (\"Receptionist\") document format: the normative JSON Schema, the cross-language conformance corpus, and the generated TypeScript package + Rust crate.",
  "packageManager": "pnpm@10.13.1",
  "engines": { "node": ">=22" },
  "scripts": {
    "gen": "pnpm -r gen",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}
